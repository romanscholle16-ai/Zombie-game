# Asset credits

Every model listed in `manifest.json` must be recorded here with its source and
licence before it is committed.

| key | file | source | licence |
|-----|------|--------|---------|
| _(none yet)_ | | | |

The game ships with no external models; everything currently rendered is
generated procedurally at runtime by `src/world/Props.js`, `src/world/Textures.js`
and `src/entities/Zombie.js`.
